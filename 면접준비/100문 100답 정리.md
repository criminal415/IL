### javascript란?

HTML과 CSS로 만들어진 웹을 동적으로 풍부하게 만들어주는 가벼운 웹 페이지용 스크립팅 언어 입니다.

### HTML이란?

HTML(Hyper Text Markup Language) 이란 단순히 text를 읽어 내려가는 순차적 접근법이 아닌 다른 페이지로 이동하거나, 같은 페이지 내의 다른 데이터로 이동할 수 있도록, 마크업 언어를 통해 구조적으로 표현할 수 있도록 컴퓨터에게 명령하고, 표시하여 전달하는 언어입니다.

### Tag란?

HTML 에서 Tag는 HTML 문서를 이루는 문법적 표시이다.

### HTML editor란?

인터넷 웹페이지를 작성하기 위한 편집도구로서, 태그를 통해 편집한 후 미리보기 기능등과 같은 것들을 지원해 개발 작업을 편하게 도와주는 도구이다.

### typescript란?

MS에서 개발하고 관리하는 자바스크립트에 타입을 부여한 오픈소스 프로그래밍 언어입니다. 타입스크립트는 정적 타입 언어이기 때문에 컴파일 시 시간이 조금 걸리더라도 안정성을 보장합니다.

### typescript를 쓰는 이유?

서로 다른 타입으로 인한 에러의 사전 방지가 가능하며 코드 가이드 및 자동완성으로 안전성과 개발 생산성을 향상 시킬 수 있다.

### 전처리기(pre-processor)

타입스크립트 파일에 선언된 import문이나 외부 호출 선언이 있을 때 참조할 수 있는 파일을 가져와 정렬된 파일 목록을 생성하는데, 이때, .d.ts보다 .ts 파일을 우선적으로 호출한다. 즉, 컴파일러는 전처리기로부터 생성된 파일 목록을 이용해 파일을 호출하고 컴파일을 수행한다.

### 컴파일러란?

특정 프로그래밍 언어로 쓰여 있는 문서를 다른 프로그래밍 언어로 옮기는 언어 번역 프로그램을 말한다 주로 C언어

### 인터프리터란?

프로그래밍 언어의 소스 코드를 바로 실행하는 컴퓨터 프로그램 또는 환경을 말한다.(실행과 동시에 줄 단위로 번역) 주로 python, javascript

### JIT 컴파일이란?

JIT 컴파일(just-in-time compilation) 또는 동적 번역(dynamic translation)은 프로그램을 실제 실행하는 시점에 기계어로 번역하는 컴파일 기법이다.(node.js는 이 방법을 사용한다.)
인터프리터와 가깝고, 중복되는 함수를 캐싱하여 반복하여

### 프로세스란?

실행중에 있는 프로그램을 의미한다. 스케쥴링의 대상이 되는 작업(task)와 같은 의미로 쓰이며, 프로세스 내부에는 최소 하나의 쓰레드(thread)를 가지고 있고 실제로 쓰레드 단위로 스케줄링을 한다.
하드 디스크에 있는 프로그램을 실행하면, 실행을 위해서 메모리 할당이 이루어지고, 할당된 메모리 공간으로 바이너리 코드가 올라가게 된다. 이순간 부터 프로세스라 불린다.

### CPU 스케줄링

CPU 할당 순서 및 방법을 결정하는 일이며, 스케줄링 알고리즘을 통해서 이루어진다. 스케줄링 알고리즘에는 대표적으로 우선순위 알고리즘과 라운드 로빈 알고리즘을 혼합하여 사용한다.

### Throughput

단위시간당 처리하는 일의 양을 말하며(네트워크에서는 단위 시간당 전송한 양을 의미 Mbps, Kbps) instructions per second라고 해서 초당 몇 million의 명령을 수행 했는가 등의 단위를 말한다. 즉 주어진 시간 동안 몇개의 프로세스를 수행했는가를 나타낸다.

### 쓰레드란?

컴퓨터에서 task로 사용하는 단위로서, 스레드는 프로세스 내부의 작업의 흐름, 단위입니다.

### 멀티쓰레드란?

쓰레드가 여러개 존재하는것을 말하며, 각 쓰레드끼리는 프로세스의 일정 메모리 영역을 공유합니다.

### 프로세스의 상태?

new, ready, running, waiting, terminated
new와 terminated는 프로세스가 만들어지거나 모두 수행되어 종료할때 잠시 생기는 state이며, 이외의 세개의 상태가 돌아가면서 프로세스가 수행되는 것이다.

- running : CPU에서 수행이 되고 있는 상태를 말한다.
- Ready : running할 준비가 되어 있는 것을 말하며 CPU가 만약 하나라면 다른 한개의 프로세스가 실행중이기 때문에 대기 상태로 이해할 수 있다.
- waiting : Ready 와 유사하지만 I/O나 다른 이벤트가 발생하기를 기다릴때를 나타낸다.

### 인터럽트란?

CPU의 정상적인 프로그램 실행을 방해했다는 의미로 쓰이며 내부 인터럽트와 외부 인터럽트 두가지 종류가 있다.

- 내부 인터럽트
  - 하드웨어 고장(Hardware Interrupt)
  - 실행할 수 없는 명령어
  - 명령어 실행 오류
- 외부 인터럽트
  - 입출력 인터럽트 : 속도가 느린 입출력장치가 입출력 준비가 완료되었음을 알리기 위해 인터럽트를 요청

### 동기와 비동기의 차이?

동기는 요청을 보낸 후 응답을 받아야지만 다음 동작이 이루어지는 방식이다. 모든 일은 순차적으로 실행되며 어떤 작업이 수행중 이라면 다음 작업은 대기합니다. 반면에 비동기는 요청에 대한 응답과 관계없이 다음 동작을 실행하는 방식을 의미한다. 나중에 들어온 요청이라도 먼저 완료되면 먼저 반환한다.

### 왜 비동기적으로 처리하는가?

자바스크립트는 싱글 쓰레드 언어입니다. 즉, 이벤트를 처리하는 call stack이 하나뿐이라는 소리이고 따라서 여러가지 이벤트를 처리할 때 동기적으로 처리하게 된다면 어떤 요청이 시간이 오래 걸릴 경우 모든 요청이 계속 delay되어 정상적으로 요청을 수행하지 못하기 때문입니다.

### 비동기 흐름을 처리하는 방식에는 무엇이 있나?

- 콜백 함수 사용(일명 콜백 지옥)
  처리되어야 하는 이벤트들을 순차적으로 콜백 함수에 넣어주는 방식이다. 즉, 함수의 반환 값을 받아서 다음 비동기 처리를 해야하는 경우에 다음 함수를 콜백으로 받아 비동기 처리를 할 수 있다. - 단점 가독성이 매우 떨어진다. - 에러처리를 한다면 모든 콜백에서 각각 에러 핸들링을 해주어야 할 것이다.
- Promise 객체
  콜백 지옥을 해소 하기 위해 ES6에서 추가된 것이며 Promise 객체는 비동기 작업이 맞이할 미래의 완료 또는 실패와 그 결과 값을 나타낸다. Promise의 상태 값에는 Pending, fulfilled, Rejected, Settled 의 4가지 상태값을 가지며, - pending : 아직 결과 값이 반환되지 않은 진행 중인 상태 - fulfilled : 성공 - Rejected : 실패 - Settled : 결과 값이 성공 혹은 실패로 반환된 상태
  의 의미를 가진다.
- async/ await
  ES2017에 추가되었으며 promise를 기반으로 사용되고, 비동기 코드를 좀더 동기적인 코드처럼 작성할 수 있게 해준다.

### Promise chaning

Promise는 객체를 반환하기 때문에 then, catch method를 사용할 수 있다는 것을 뜻하며, 이를 통해 연속적으로 then 메소드를 이용하여 Promise chaining이 가능하다.

### Promise.all

Promise.all() 메서드는 순회 가능한 객체에 주어진 모든 프로미스가 이행한 후, 혹은 프로미스가 주어지지 않았을 때 이행하는 Promise를 반환합니다.

### http통신에 대해서?

인터넷에서 데이터를 주고받을 수 있는 프로토콜입니다. header는 General, Respnse Headers, Request Headers로 Date, cache-Control, Content-Type, User_Agent, Accept, Authorization,
Origin, Referer,
Access-Control-Allow-Origin(프로토콜, 서브도메인, 도메인, 포트)

### http method

GET, POST, PUT, DELETE,
OPTIONS : 요청 URI에 어떤 method 요청이 가능한지 받을 수 있다.

### https란?

### noSQL과 SQL의 장단점

- SQL
  - 데이터는 정해진 데이터 스키마에 따라 테이블에 저장된다.
  - 데이터는 관계를 통해 여러 테이블에 분산된다.
- NoSQL - NoSQL에서는 다른 구조의 데이터를 같은 컬렉션에 추가 가능 - 관련 데이터를 동일한 컬렉션에 넣는다.
  따라서 NoSQL은 조인할 필요없이 이미 필요한 모든 것을 갖춘 document를 작성

### SQL Injection

> 해커에 의해 조작된 SQL 쿼리문이 데이터베이스에 그대로 전달되어 비정상적 명령을 실행시키는 공격 기법

### index란?

인덱스란 추가적인 쓰기 작업과 저장 공간을 활용하여 데이터베이스 테이블의 검색 속도를 향상시키기 위한 자료구조이다

### hash table이란?

해시 테이블은 (Key, Value)로 데이터를 저장하는 자료구조 중 하나로 빠른 데이터 검색이 필요할 때 유용하다. 해시 테이블은 Key값을 이용해 고유한 index를 생성하여 그 index에 저장된 값을 꺼내오는 구조이다.

### DB 정규화란?

정규화(Normalization)의 기본 목표는 테이블 간에 중복된 데이타를 허용하지 않는다는 것이다. 중복된 데이터를 허용하지 않음으로써 무결성(Integrity)를 유지할 수 있으며, DB의 저장 용량 역시 줄일 수 있다. 1,2,3 정규화와 BCNF 정규화가 있다.

- 제 1 정규화 : 테이블의 컬럼이 원자값(Atomic Value, 하나의 값)을 갖도록 테이블을 분해하는 것이다.
- 제 2 정규화 : 제1 정규화를 진행한 테이블에 대해 완전 함수 종속을 만족하도록 테이블을 분해하는 것이다. 즉, 기본키의 부분집합이 결정자가 되어선 안된다.
- 제 3 정규화 : 제2 정규화를 진행한 테이블에 대해 이행적 종속을 없애도록 테이블을 분해하는 것이다.
- BCNF 정규화 : 제3 정규화를 진행한 테이블에 대해 모든 결정자가 후보키가 되도록 테이블을 분해하는 것이다.

### key의 종류는?

- 후보키(Candidate Key) :
- 기본키(Primary Key) :
- 대체키(Alternate Key) :
- 슈퍼키(Super Key) :
- 외래키(Foregin Key) :

### 클로져란?

내부함수가 외부함수의 지역변수에 접근 할 수 있고, 외부함수는 외부함수내에서의 지역변수를 사용하는 내부함수가 소멸될 때까지 소멸되지 않는 특성을 의미한다.

### scope

- global scope

- local scope
  function scope, block scope는 지역 스코프의 예

### scope chain

### 원시타입이란?

### Auto boxing?

### V8엔진이란?

### Implicit type coercion

암시적 타입 변환이란 자바스크립트가 예상하지 못한(unexpected)값 타입을 예상되는 값 타입으로 강제 변환하려는 시도이다. 즉, 암시적 변환은 자바스크립트에 의해 자동으로 일어나는 변환이다.

> 모든 자바스크립트 객체들은 toString 메소드를 상속받는다.

> valueOf 메소드는 숫자 값을 나타내기 위한 메소드로, string또는 number 타입으로 예상되는 값에서 valueOf 메소드를 정의 할 수 있다.(toString보다 우선순위를 갖는다.)

### Explicit Type Coericion

명시적 변환이란 Type casting과 같은 의미로, 개발자가 의도를 가지고 변환할 타입을 명시적으로 변환

### Duck Typing

동적 타이핑의 한 종류로, 오리처럼 행동하면 오리로 봐도 무방하다라는 의미로 덕 타이핑이라 불린다.
장점은 타입에 대해 매우 자유롭고 런타임 환경의 데이터를 기반으로 한 기능과 자료형을 창출할 때 유리하다.
단점은 런타임 환경에서 오류가 발생할 수 있으며, 예상치 못한 유형의 값이 있을 수 있고 그러한 데이터에 대한 무의미한 작업이 적용될 수 있다. 버그를 찾기 어려울 수도 있다.

단위 테스트로 문제점 보완, 동적으로 정형된 언어의 경우 특히 중요

### Value Types and Reference Types

Value Type : 자바스크립트에서 원시 타입인 boolean, null, undefined, string, number 타입은 값에 의한 전달(passed by value)이 일어난다.

Reference Types : 원시 타입이 아닌 Object 타입은 참조에 의한 전달(passed by reference)이 일어난다.

### 프로퍼티 & 메소드

객체를 완성하는 구성품들은 데이터를 의미하는 프로퍼티와 데이터를 참조하고 조작할 수 있는 동작을 의미하는 메소드이다.

### callback, promise, async await

### node.js란 express란?

chrome V8 javascript 엔진으로 빌드된 javascript 런타임 입니다.

### 호이스팅이란?

### 이벤트 루프?

### AICD란?

### CI/CD란?

### 객체지향언어란?

### micro task?

### session과 쿠키의 차이?

### session과 토큰의 차이?

### header란?

### preflight란?

### TCP/IP란?

### JWT?

### OAuth?

### CORS란?

### SOP란?

### 컴파일러와 인터프리터의 차이?

### 함수선언식과 표현신?

### 프로세스란?

### 프레임워크란?

### 멀티 프로세스?

### 멀티 쓰레드?

### OAuth란?

### Web이란?

### 프로토타입이란?

### API란

다른 어플리케이션에서 현재 프로그램의 기능을 사용할 수 있게 허용하는 접점을 의미한다.

### 웹 API란

다른 웹 서비스의 기능을 사용하거나 자원을 가져올 수 있는 창구 이다. 즉, 내가 서버를 만든다면 다른 사람에게 정보를 제공하고 싶은 부분만 API를 열어놓고, 제공하고 싶지 않은 부분은 API를 만들지 않는다. 또한 이러한 서버에 API를 올려서 URL을 통해 접근할 수 있게 만든 것을 웹 API서버라고 한다.

### Rest API란?

### micro task

### next tick

### proxy server란?

### 로드벨런싱이란?

로드밸런싱(부하분산)은 중앙처리장치 또는 저장장치와 같은 컴퓨터 자원들에게 작업을 분산하는 것을 의미합니다.

### ORM 장단점?

### DevOps란?
